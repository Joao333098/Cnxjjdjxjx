services:
  - type: web
    name: ai-browser-agent
    env: docker
    plan: starter # O plano gratuito (free) pode ter pouca memória (512MB) para rodar o Chromium, mas você pode tentar começar por ele.
    envVars:
      - key: VITE_NOVA_API_KEY
        sync: false
